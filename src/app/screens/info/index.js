import React from "react";
import Header from "../../components/organism/header";

const Info = (props) => {
  return (
    <div>
      <Header optionSelected="info" />
    </div>
  );
};

export default Info;