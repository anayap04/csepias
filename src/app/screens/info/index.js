import React from "react";
import Header from "../../components/atoms/header";

const Info = (props) => {
  return (
    <div>
      <Header optionSelected="info" />
    </div>
  );
};

export default Info;